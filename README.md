Реализация дерева Фенвика с использованием. На основе этой структуры дополнительно реализовано решение известной задачи - поиска k-й порядковой статистики в скользящем окне.

## Структура проекта

```text
pom.xml
.github/
  workflows/
    java-ci.yml
src/
  main/
    java/
      slidingk/
        FenwickTree.java
        SlidingKStatistic.java
        Main.java
  test/
    java/
      slidingk/
        FenwickTreeTest.java
        SlidingKStatisticTest.java
```


## Корневые файлы

pom.xml
Конфигурация Maven-проекта: настройки компилятора (Java 17), подключение JUnit 5 для тестов. .github/workflows/java-ci.yml
Конфигурация GitHub Actions. При каждом push / pull request:
собирает проект
запускает тесты (mvn test)
помечает сборку как успешную только при прохождении всех тестов.



## Основные классы (src/main/java/slidingk)

FenwickTree.java.

Реализация дерева Фенвика.
хранение данных только во внутреннем массиве tree.
методы build, update, prefixSum, rangeSum.
дополнительно реализован метод поиска по порядку (используется для k-й статистики);
предусмотрены проверки аргументов и выброс стандартных исключений (IllegalArgumentException, IndexOutOfBoundsException, IllegalStateException) в некорректных ситуациях.
SlidingKStatistic.java
Класс, использующий FenwickTree как дерево частот по сжатым значениям:
поддерживает расширение окна вправо и сдвиг влево;
для каждого состояния окна позволяет получить k-ю порядковую статистику;
служит примером практического применения дерева Фенвика помимо сумм на отрезках.
Main.java
Консольное приложение:
считывает входные данные (размер массива, число операций, параметр k, сам массив и строку операций);
выполняет координатное сжатие значений;
создаёт объект SlidingKStatistic и обрабатывает последовательность операций L / R;
выводит k-ю порядковую статистику после каждой операции.


## Тесты (src/test/java/slidingk)

FenwickTreeTest.java
Набор модульных тестов для FenwickTree:
проверка корректности построения, обновлений, префиксных и диапазонных сумм;
тесты для метода поиска по порядку на частотных массивах;
отдельные тесты на выброс исключений при некорректных аргументах и граничных случаях.
SlidingKStatisticTest.java
Тесты для логики скользящей k-й статистики:
различные сценарии последовательностей L / R;
случаи с дубликатами в массиве;
проверки для k = 1, k = |окна| и k больше текущего размера окна.


## Условие задачи: О скользящей k-статистке


По массиву двигаются два указателя `l` и `r`, которые изначально направлены на самый первый элемент массива. Есть две операции: `L` — сдвинуть указатель `l` на один элемент вправо и `R` — сдвинуть указатель `r` на один элемент вправо. После выполнения каждой из операций нужно вывести k-е в порядке возрастания число среди всех чисел от `l` до `r` включительно, либо `-1`, если всего чисел от `l` до `r` меньше, чем `k`.

Формат ввода
В первой строке входа — три целых числа `n, m, k`. Во второй строке `n` целых чисел, задающих массив чисел, по которому будут двигаться два указателя `l` и `r`. В третьей строке входного файла — `m` символов `R` или `L`, без пробелов, в одну строку. Это порядок выполняемых операций. 

Гарантируется, что указатель `l` никогда не "обгоняет" указатель `r`. Гарантируется, что указатель `r` никогда не выйдет за пределы массива. При этом `1 ≤ n, k ≤ 100000`, `0 ≤ m ≤ 2n − 2`. Все числа в массиве неотрицательные и не превосходят `10^9`.

Формат вывода
Выведите ровно `m` строк, в каждой — ровно по одному целому числу. После выполнения каждой из операций нужно вывести k-е в порядке возрастания число среди всех чисел от `l` до `r` включительно, либо `-1`, если всего чисел от `l` до `r` меньше, чем `k`.